// ignore: file_names
import 'package:flutter/material.dart';

class TitleContactList extends StatelessWidget {
  const TitleContactList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "My Contacts",
      style: TextStyle(color: Colors.black, fontSize: 18.0),
    );
  }
}
